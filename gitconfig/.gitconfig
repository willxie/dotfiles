[user]
        name = Will Xie
        email = will.xie@getcruise.com
[core]
        editor = vim
[push]
        default = simple
[filter "lfs"]
        clean = git-lfs clean -- %f
        smudge = git-lfs smudge -- %f
        process = git-lfs filter-process
        required = true
